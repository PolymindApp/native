import { THEME } from '@polymind/sdk-js';

const tintColor = THEME.primary;

export default {
    tintColor,
    tabIconDefault: '#ccc',
    tabIconSelected: tintColor,
    tabBar: '#fefefe',
    errorBackground: THEME.error,
    errorText: '#fff',
    warningBackground: THEME.warning,
    warningText: '#666804',
    noticeBackground: tintColor,
    noticeText: '#fff',
};
